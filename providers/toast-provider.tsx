"use client";

///this is the toaster provider for the message notice of "added to cart"
import { Toaster } from "react-hot-toast";

const ToastProvider = () => {
	return <Toaster />;
};

export default ToastProvider;
